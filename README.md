# Dylumo: Multimodal Image-to-Music Recommendation System

A system that analyzes images and recommends music that matches the mood and context.

## Quick Start

1. Install dependencies:
```bash
pip install -r requirements.txt
```

2. Run the example:
```bash
python src/example_basic.py
```

## Project Structure

```
├── src/              # Main source code
├── notebooks/        # Jupyter notebooks for experiments
├── data/             # Datasets (not in git)
├── experiments/      # Experiment results
├── docs/             # Documentation
├── docker/           # Docker configuration
├── paper/            # Research paper (LaTeX)
└── prompts/          # GPT prompts log
```

## Setup Instructions

1. Create virtual environment:
```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

2. Install dependencies:
```bash
pip install -r requirements.txt
```

## Authors

- Muhammad Awab ur Rehman (22i-1068)
- Adeel Mehmood (22i-0979)
- Talha Azim (22i-1243)

